import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    height: 48, // burası dimensionstan gelcek
    backgroundColor: '#F8F8F6',
    paddingHorizontal: 13,
    borderRadius: 14,
    paddingVertical: 14,
    flexDirection: 'row',
    alignItems: 'center',
  },
  input: {marginLeft: 16},
});

export default styles;
